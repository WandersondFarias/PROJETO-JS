Math é um objeto embutido que tem propriedades e métodos para constantes e funções matemáticas.

- console.log(Math.PI) // Mostra o valor de Pi (3,14...)
- console.log(Math.pow(2,3)) // Isso determina o valor elevado, ou seja, 2³
- console.log(Math.sqrt(25,2)) // Raiz quadrada de 25, mas pode ser cúbica, quarta, quinta e etc
- console.log(Math.random()) // Mostra um número aleatório entre 0 e 1
- console.log(Math.ceil(23.9)) // Arredondar o número para "cima". Não pode usar vírgula, então utilize o ponto no lugar.
- console.log(Math.floor(23.7)) // Arredonda o número para "baixo". Não pode usar vírgula, então utilize o ponto no lugar.